name: Unity CI

on: [push, pull_request, workflow_dispatch]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup Unity and activate license
      - name: Setup Unity
        uses: game-ci/unity-setup@v2
        with:
          unityVersion: '2022.3.4f1'
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}

      # Build the Unity project
      - name: Build project
        uses: game-ci/unity-builder@v4
        with:
          unityVersion: '2022.3.4f1'
          targetPlatform: StandaloneWindows64 # Replace with your desired target platform
        env:
          UNITY_SERIAL: ${{ secrets.UNITY_SERIAL }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}

      # Deactivate Unity license (optional cleanup)
      - name: Deactivate Unity
        uses: game-ci/unity-return-license@v1
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
